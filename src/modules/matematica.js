const PI = 3.14;
function sumar(x, y) {
    return x + y;
}
const multiplicar = (a, b) => {
    return a * b;
};

const dividir = (a, b) => {
    return a / b;
}

function restar(a, b) {
    return a - b;
}

const arrayNums = ["dos", "cuatro", "seis", "ocho", "diez"];

export {PI, sumar, multiplicar, dividir, restar, arrayNums};